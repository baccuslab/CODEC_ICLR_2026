# CODEC_ICLR_2026

- one directory per figure
- each directory contains:
    - scripts: containing the code to generate the figure
    - panels: containing the saved .svgs that compose a figure
    - the final .svg
